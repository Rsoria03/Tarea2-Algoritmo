function ContarCaracteres() {
    let frase, cl, longf;
    frase = prompt("Ingrese una frase: ");
    longf = frase.length;
    alert (`${frase} \nLa frase tiene ${longf} caracteres`);
}ContarCaracteres();